# IF Stataments
cars = ['audi', 'bmw', 'subaru', 'toyota']
for car in cars:
    if car == 'bmw': # Python uses columns C uses paranthesis
        print(car.upper())
    else:
        print(car.title())